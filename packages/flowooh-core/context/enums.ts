/**
 * Status is the status of the token/context
 */
export enum Status {
  Ready = 'ready',
  Paused = 'paused',
  Failed = 'failed',
  Running = 'running',
  Completed = 'completed',
  Terminated = 'terminated',
}
